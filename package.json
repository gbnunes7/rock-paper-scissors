{
  "scripts": {
    "build:css": "postcss src/input.css -o src/output.css",
    "build": "npm run build:css && <outro comando de build, se necessário>",
    "start": "npm run build && vercel dev"
  },
  "devDependencies": {
    "autoprefixer": "^10.2.5",
    "postcss": "^8.2.6",
    "tailwindcss": "^2.1.2"
  }
}